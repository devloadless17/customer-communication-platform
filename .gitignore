# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
.next
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.imitate

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# turbo (monorepo task cache)
.turbo

# editor
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.env.local-docker

# Claude Code session/memory data (skills under .claude/skills ARE tracked; this is not)
.claude/projects/
# Claude Code runtime locks (transient scheduler/loop artifacts — never commit)
.claude/*.lock
# Persisted Workflow scripts dropped at the .claude root (one-off audit/agent
# harnesses — runtime-only DSL, not app source). Skills under .claude/skills/ stay tracked.
.claude/*.mjs
# Per-machine Claude Code permission overrides (personal, never team-wide)
.claude/settings.local.json

# Playwright auth storage (per-machine session cookies)
tests/e2e/.auth/
test-results/
playwright-report/


# Meta e2e stack logs (mock Graph + isolated test api)
tests/e2e/.meta-logs/

# Generated from apps/web/public/widget.js by prebuild (see scripts/build-widget.mjs)
apps/web/public/widget.min.js
