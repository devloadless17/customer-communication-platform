/**
 * Placeholder ESLint config. Adding real rules is a deliberate follow-up
 * PR — the monorepo refactor is not the place to wedge in lint policy.
 */
module.exports = {
  root: true,
  rules: {},
};
